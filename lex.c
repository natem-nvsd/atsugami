#include "atsugami.h"
#include <stdio.h>
#include <string.h>

extern char *search_input_sanitiser(const char *str) {
	char *query;
	
	return query;
}

extern char *tag_input_sanitiser(const char *str) {
	char *sanitised_str;

	return sanitised_str;
}

extern long word_count(const char *str) {
	long wc;
	wc = 0;

	return wc;
}
